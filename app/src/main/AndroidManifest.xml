<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drazzer.tourismo">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Main.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main.MainActivity">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".TimeConverter.TimeMain" />
        <activity android:name=".CurrencyConverter.currencyList" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps.MapsActivityMain"
            android:label="@string/title_activity_maps_main" />
        <activity android:name=".CurrencyConverter.CurrencyConverter" />
        <activity android:name=".Weather.Weather_Activity_Main" />
        <activity android:name=".Translator.Translator_main" />
        <activity
            android:name=".SocialDinning.SocialDinningMain"
            android:label="@string/title_activity_social_dinning_main" />
        <activity android:name=".UploadImageVideo.UploadingImageMain" />
        <activity android:name=".TimeConverter.Time_Zone_List" />
        <activity android:name=".SocialDinning.ProfileOrChefList" />
        <activity android:name=".ContactServices.ContactServicesMain" />
        <activity android:name=".ContactServices.ContactsListView.ContactListViewMain" />
        <activity android:name=".ItineraryBuilder.ItineraryMain" />
        <activity android:name=".ContactServices.ChatServices.ChatMain" />
        <activity android:name=".ItineraryBuilder.TraveSelectionMenu.TravelSelectionMenu" />
        <activity android:name=".ItineraryBuilder.TraveSelectionMenu.Transport.Transport_main" />

    </application>

</manifest>